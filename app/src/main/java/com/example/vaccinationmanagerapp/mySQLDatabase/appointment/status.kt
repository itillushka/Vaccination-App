package com.example.vaccinationmanagerapp.mySQLDatabase.appointment

/**
 * This enum class represents the possible statuses of an Appointment in the system (Scheduled, Completed, Canceled).
 */
enum class status {
    Scheduled,
    Completed,
    Canceled
}